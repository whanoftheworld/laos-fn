# === Dependencies ===
node_modules
.pnpm-store
.npm
.yarn

# === Build outputs ===
dist
build
dist-ssr
out
.next
.nuxt
.cache
.parcel-cache
.vite
.turbo
.svelte-kit
*.tsbuildinfo

# === Coverage / test outputs ===
coverage
.nyc_output

# === Environment / secrets ===
# .env files: only commit .env.example
.env
.env.local
.env.*.local
.env.production
.env.development

# Claude Code MCP config — contains GitHub PAT + DigitalOcean API token + SSH paths
# Use .mcp.json.example as the safe template
.mcp.json

# Claude Code per-machine local settings
.claude/settings.local.json
.claude/.cache/

# Cloudflare tunnel credentials & cert (TunnelSecret lives here)
cloudflared/*.json
cloudflared/cert.pem
cloudflared/logs/
*.cloudflared.json

# Generic secret patterns (defense-in-depth)
**/secrets.json
**/secrets.yml
**/secrets.yaml
**/*.secret.*
**/credentials.json
**/serviceAccount*.json
*.pem
*.key
!**/public.pem
!**/public.key

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
lerna-*.log

# === OS noise ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# === Editors / IDEs ===
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
.idea/
*.iml
*.sublime-workspace
.history/
.ionide/

# === Reference assets (large local-only screenshots) ===
docs/reference/*.jpeg
docs/reference/*.jpg
docs/reference/*.png
docs/reference/*.gif
docs/reference/*.webp
docs/reference/*.txt
!docs/reference/.gitkeep
!docs/reference/README.md

# === Temp / backup ===
*.tmp
*.temp
*.bak
*.swp
*.swo
*~

# === Local overrides ===
*.local

# === Repo-only exclusions (project code only — keep these local) ===
# Claude Code AI assistant guide + skills + commands
CLAUDE.md
.claude/

# MCP config (real file already ignored; the example template stays local too)
.mcp.json.example

# Cloudflare Tunnel dev infra (machine-specific, not part of the app)
cloudflared/
